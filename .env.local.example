# Development Environment Configuration
#
# Copy this file to .env.local and configure your local development settings.
# .env.local is gitignored and will NEVER be committed to the repository.
#
# Usage:
#   cp .env.local.example .env.local
#   # Edit .env.local with your values
#
# IMPORTANT: These settings only work in development builds (npm run dev)
#            They are NOT included in production builds (npm run build)

# ===================================================================
# Blockstream API Configuration
# ===================================================================

# Production: AWS Lambda Proxy URL (API key secured on backend)
# This is the SECURE way to use Blockstream API in production.
# The Lambda proxy holds the API key server-side, preventing extraction.
#
# Architecture:
#   Extension → Lambda Proxy → Blockstream API
#                (API key here)
#
# Set this in production builds or when testing the Lambda proxy locally.
# Example: BLOCKSTREAM_PROXY_URL=https://api.yourdomain.com/blockstream
#
# Leave empty or comment out to use Blockstream API directly (development only)
BLOCKSTREAM_PROXY_URL=

# ===================================================================
# Development Auto-fill Configuration
# ===================================================================

# Auto-fill password for wallet unlock screen (development only)
# This saves you from typing your password every time you test the wallet
# Leave empty or comment out to disable auto-fill
DEV_PASSWORD=

# Auto-fill mnemonic phrase for wallet import screen (development only)
# This saves you from typing your test seed phrase when testing import functionality
# Use a 12, 15, 18, 21, or 24 word BIP39 mnemonic phrase
# Leave empty or comment out to disable auto-fill
DEV_MNEMONIC=

# ===================================================================
# Example Configurations
# ===================================================================

# Development (Direct Blockstream API):
# DEV_PASSWORD=MyTestPassword123
# DEV_MNEMONIC=abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about
# BLOCKSTREAM_PROXY_URL=

# Development (Test Lambda Proxy Locally):
# DEV_PASSWORD=MyTestPassword123
# DEV_MNEMONIC=abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about
# BLOCKSTREAM_PROXY_URL=https://your-staging-lambda.execute-api.us-east-1.amazonaws.com/prod/blockstream

# Production (Lambda Proxy Only):
# Set via build environment variable:
# BLOCKSTREAM_PROXY_URL=https://api.yourdomain.com/blockstream npm run build
